# empire-research

Skills installed under the `empire-research` Claude Code plugin.

- `/empire-research:explore` and `/empire-research:compare` are recommendation-only — never apply edits, commit, or run side effects. Wait for the user to pick a direction before any code change.
- Findings stay local — never post to Slack, GitHub, Jira, or external systems.
